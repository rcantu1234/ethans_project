<%= form_for(@letter) do |f| %>
  <% if @letter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@letter.errors.count, "error") %> prohibited this letter from being saved:</h2>

      <ul>
      <% @letter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :first_image %><br>
    <%= f.text_field :first_image %>
  </div>
  <div class="field">
    <%= f.label :second_image %><br>
    <%= f.text_field :second_image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
